# Database and uploaded files (DO NOT COMMIT THESE)
static/uploads/
static/uploads/guides/
*.db
*.sqlite3
*.sqlite
papers/

# Python environments and cache
venv/
venv_new/
.venv/
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.pytest_cache/

# Environment variables
.env
.env.prod
.env.*.local
.claude/

# IDEs and Editors
.vscode/
.idea/
*.swp
*.swo

# MacOS files
.DS_Store
.AppleDouble
.LSOverride

# TLS / SSL keys (DO NOT COMMIT)
*.key
*.pem
*.crt
*.cert

# Data — runtime files
data/active_sessions.json
data/active_sessions
data/pending_papers/
data/papersz

# Curriculum / category configs — runtime-editable via the manage pages; admin/local
# edits must NOT sync. The repo ships *.sample.json baselines; the app self-seeds the
# live file from config.py defaults on first run (or copy the sample over it). Don't re-track.
data/ee_subjects.json
data/ia_subjects.json
data/paper_categories.json
data/news_categories.json
data/guide_categories.json

# Superpowers plans/specs (local only)
docs/review
docs/superpowers
LLM_DEPLOYMENT_IDEAS.md

# Utility / migration scripts
test_db.py
remigrate_unicode.py
update_db.py

# Academic resources file blobs
resource_files/
AI_Prompts/