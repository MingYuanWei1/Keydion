# Secrets
.env*
!.env.example

# TLS / SSL key material
*.key
*.pem
*.crt
*.cert
*.p12
*.pfx

# Local artifacts
local/

# Local development entry points
start_local.sh
docker-compose.yml

# Runtime data & user uploads
static/uploads/
papers/
resource_files/
data/pending_papers/
data/version_update_state.json
*.db
*.sqlite
*.sqlite3

# Curriculum / category configs — runtime-editable via the manage pages; admin/
# local edits must NOT sync. The repo ships *.sample.json baselines; the app
# self-seeds the live file from config.py defaults on first run (or copy the
# sample over it). Don't re-track.
data/ee_subjects.json
data/ia_subjects.json
data/paper_categories.json
data/paper_journals.json
data/news_categories.json
data/guide_categories.json

# ─── Operator backups, DB exports, and ad-hoc archives ──────────────────────
backups/
*.sql
*.sql.gz
*.dump
*.backup
*.tar
*.tar.gz
*.tgz
*.zip
!tests/fixtures/**/*.zip

# ─── AI assistant scaffolding ───────────────────────────────────────────────
.claude/
.qoder/
.codex/
.zcode/
.gemini/
.superpowers/
AI_Prompts/
docs/review/
docs/superpowers/

# ─── Python ─────────────────────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# ─── IDEs & editors ─────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ─── macOS ──────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
