# Extract with `python tools/extract_translations.py`, NOT a bare `pybabel
# extract`: Babel's default directory filter skips dirs whose name starts with
# `.` or `_` (e.g. templates/_dashboard/), so a plain extract drops them.
[python: **.py]
[jinja2: templates/**.html]
