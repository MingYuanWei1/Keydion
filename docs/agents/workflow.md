# Git Workflow

- Use brief, targeted conventional commits such as `feat: ...` or `fix(scope): ...`.
- `docs/plans/` and `docs/specs/` are local, gitignored planning areas. Never stage or commit their contents.
